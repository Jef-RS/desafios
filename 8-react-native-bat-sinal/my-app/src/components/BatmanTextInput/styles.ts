import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  label: {
    fontSize: 12,
    fontWeight: "bold",
    marginBottom: 8,
  },
  input: {
    borderWidth: 2,
    borderRadius: 4,
    borderColor: "#0f0f0f",
    padding: 12,
    marginBottom: 12,
  },
});
