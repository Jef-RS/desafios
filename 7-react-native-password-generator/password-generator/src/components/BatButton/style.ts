import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    minWidth: "49%",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#EBAE6A",
    borderRadius: 4,
    padding: 12,
  },
  title: {
    fontSize: 12,
    fontWeight: "bold",
  },
});
